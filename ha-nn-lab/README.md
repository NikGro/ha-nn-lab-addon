# HA NN Lab Add-on

Home Assistant add-on with:
- Multiple NN projects
- Per-project shadow mode (default ON)
- Manual + all-entities selection for sensors/actuators
- Live suggestion + feedback loop
- NN visual graph per project
